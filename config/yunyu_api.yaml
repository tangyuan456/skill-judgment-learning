# YunYu HTTP API（yunyu_test_results 统一查询入口）
# 本 Skill 默认走 HTTP API（无需直连 PostgreSQL），已内置 URL/Token/SysID/PID。
# 如需覆盖，可：
#   1) 修改本文件
#   2) 设置环境变量：YUNYU_API_URL / YUNYU_API_SYS_ID / YUNYU_API_TOKEN / YUNYU_API_PID

yunyu_api:
  url: http://yunyu.woa.com/test/api/alarm/sql_query/
  sys_id: check
  token: 
  p_id: 1
  timeout: 60
